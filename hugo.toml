baseURL = 'https://fluidity.build/'
languageCode = 'en-us'
title = 'fluidity'
theme = 'hugo-bearblog'
enableRobotsTXT = true
disableKinds = ['taxonomy', 'term']
ignoreErrors = ['error-disable-taxonomy']

[params]
  title = 'fluidity'
  description = 'A local-first desktop workbench for building customized AI-assisted development workflows with projects, workspaces, tiles, and extensions.'
  favicon = 'favicon.svg'
  hideMadeWithLine = true

[markup]
  [markup.highlight]
    style = 'friendly'
    lineNos = false
    codeFences = true

  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
